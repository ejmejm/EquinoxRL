defaults:
  - defaults

model:
  activation: tanh

# Task parameters
task:
  name: nonlinear_geoff
  type: regression
  n_real_features: 10
  flip_rate: 0.005
  n_layers: 2
  hidden_dim: 20
  weight_scale: 1.0
  activation: tanh
  sparsity: 0.00